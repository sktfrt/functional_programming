# ЛЯМБДА - ИСЧЕСЛЕНИЕ

## Функциональная модель вычисления
> Императивное программирование:
> - последовательно
> - изменяемые переменные

> Функциональное прграммироание - это выражение:
> - редексы

```cpp
2 * 7 + 3 --> 14 + 3 --> 17
1 редекс -> 1 -> done
```

**символ равенства(=) называется связывание или равенстов по определению(definitinal equality), полностью иммутабельно**

`x = 2 * x + 3` - пример рекурсивного вычисления

анонимная фукнция - 
`\y -> 2 * y + 3`
или на мат языке `$\lambda$y.2y + 3` (абстрактор.функция) 

синтаксис применения:
```
(\y -> 2 * y + 3) 4
(\y -> 2 * y + 3) (4 + 3)
```
выражение вида F A (функция выражение) называется бета редексом 
`foo = \y -> 2 * y + 3`
с помощью связывние можно связывать лямбды

`(\y -> 2 * y + 3) (4 + 6)` - а что первым сокращать? 
- ленивая стратегия: сокращает самый левый(в хаскеле)
`2 * (4 + 6) + 3`
- энергичня стратегия: 
`(\y -> 2 * y + 3) 10`

```haskell
fact = \n if n == 0 then 1 else n * fact (n - 1)
fact 3 -->
(\n if n == 0 then 1 else n * fact (n - 1)) 3 -->
if 3 == 0 then 1 else 3 * fact (3 - 1) --> ...
```

## чистое $\lambda$ - исчисления

> **Опр.** Множетсво $\lambda$ ьермов Л индуктивно строится из переменных V = {x, y, z, ...}
пример лямбда терм: x, y, z, (xy), (z(xy)), ((xy)z)

Соглашения:
- произвольные термы - заглавными, конкретные - маленькими
- FXYZ == (((FX)Y)Z)
- $\lambda$xyz.M == ($\lambda$x($\lambda$y($\lambda$z(M))))
- $\lambda$x.FXY == $\lambda$x.(FXY)
  
`($\lambda$x.M) N --> [x := M] N`

`z(xy)` - x свободный, `$\lambda$z(xy)` - связанный 
почле связывания переменная становится *локальной*
`($\lambda$x.($\lambda$x.xz)x)x` 

> Комбинатор - если в нем нет свободных терм
нек комбинаторы имеют общепринитые имена(см. през)

имена свзяанных переменных можно менять

### КомбинАторная логика
без лямбд,досаточно лямбд S и K (SKK == I)

## Подстановка и отношения редукции
подставновка выполняется только вместо свободных переменных
`[x := y]($\lambda$y.xy)` == $\lambda$y.yy - первый *y* больше не свободный!!!!
поэтому просто переименовываем 

### Лемма подстановки
> см през, спрашивают контр пример

и еще 7 слайдов....